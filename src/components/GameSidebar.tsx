
"use client";

import React from 'react';

// This component is no longer used as its content has been moved into the ModernAppMenubar.
// It is kept to avoid breaking potential imports but can be safely deleted.
const GameSidebar: React.FC = () => {
  return null;
};

export default GameSidebar;
